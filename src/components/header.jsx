import React from "react";

function Header() {
    return (
        <header>
            <div className="text-header card-wrapper animated">
                GitHub repositories viewer
            </div>
        </header>
    );
}

export default Header;